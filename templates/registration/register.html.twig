{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<div class="profil-page">
    <h1>Profil de {{ users.pseudo }}</h1>

    <h2>Vos likes :</h2>
    {% if likedFilms is empty %}
        <p class="no-likes">Vous n'avez pas encore liké de films.</p>
    {% else %}
        <ul class="liked-films-list">
            {% for like in likedFilms %}
                <li class="film-item">
                    <div class="film-info">
                        <a href="{{ path('film_show', {'id': like.film.id}) }}" class="film-title">
                            {{ like.film.nom }}
                        </a>
                        <!-- Formulaire de Dislike -->
                        <form action="{{ path('app_film_dislike', {'id': like.film.id}) }}" method="POST">
                            <button type="submit" class="dislike-button">👎 Dislike</button>
                        </form>
                    </div>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
</div>
{% endblock %}
