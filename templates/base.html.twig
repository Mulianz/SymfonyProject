<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Bienvenue{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/styles.css') }}">
    {% endblock %}
</head>
<body>
    <header>
        <div class="header-container">
            <!-- Logo -->
            <div class="logo">
                <a href="/" class="home-link">
                    <span>🎬</span> MovieTime
                </a>
            </div>

            <!-- Navigation -->
            <nav class="navigation">
                <a href="/" class="nav-link">Accueil</a>
                <a href="/personnages" class="nav-link">Personnages</a>
            </nav>

            <!-- Afficher pseudo ou connexion -->
            <div class="header-profile">
                {% if app.session.get('users') %}
                    <!-- Afficher le pseudo de l'utilisateur connecté -->
                    <a href="/profile" class="profile-link">
                        <div class="profile-icon-container">
                            <div class="profile-icon"></div>
                        </div>
                        <span class="profile-text">{{ app.session.get('users').pseudo }}</span>
                    </a>
                    <!-- Bouton de déconnexion -->
                    <a href="{{ path('app_logout') }}" class="profile-link">
                        <div class="profile-icon-container">
                            <div class="profile-icon"></div>
                        </div>
                        <span class="profile-text">Déconnexion</span>
                    </a>
                {% else %}
                    <!-- Icône de connexion -->
                    <a href="/login" class="profile-link">
                        <div class="profile-icon-container">
                            <div class="profile-icon"></div>
                        </div>
                        <span class="profile-text">Connexion</span>
                    </a>
                {% endif %}
            </div>
        </div>
    </header>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer>
        <div class="footer-container">
            <p class="footer-text">&copy; 2024 MovieTime. Tous droits réservés.</p>
        </div>
    </footer>

    {% block javascripts %}
    <!-- Ajouter ici vos fichiers JavaScript si nécessaire -->
    {% endblock %}
</body>
</html>
