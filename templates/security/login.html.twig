{% extends 'base.html.twig' %}

{% block body %}
<div class="login-page">
    <div class="login-block">
        <h1 class="login-title">🎥 Connexion</h1>
        <p class="login-subtitle">Rejoignez l'univers du cinéma</p>
        
        <form method="post" class="login-form">
            {% if error %}
                <div class="login-error">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}
            <div class="form-group">
                <label for="username" class="form-label">Email :</label>
                <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-input" placeholder="Entrez votre email">
            </div>
            <div class="form-group">
                <label for="password" class="form-label">Mot de passe :</label>
                <input type="password" id="password" name="_password" class="form-input" placeholder="Entrez votre mot de passe">
            </div>
            <div class="form-actions">
                <button type="submit" class="login-button">Se connecter</button>
            </div>
        </form>

        <div class="login-footer">
            <p class="signup-prompt">Pas encore inscrit ? <a href="/register" class="signup-link">Créer un compte</a></p>
        </div>
    </div>
</div>
{% endblock %}
